# dependencies — contain >100MB binaries GitHub rejects
node_modules/

# next.js build cache — contains >100MB cache files GitHub rejects
.next/

# env files (can opt-in for committing if needed)
.env*
